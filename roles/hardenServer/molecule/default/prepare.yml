---
- name: Prepare
  hosts: all
  become: true
  tasks:
    - name: Update apt cache
      ansible.builtin.apt:
        update_cache: true

    - name: Install OpenSSH server
      ansible.builtin.apt:
        name: openssh-server
        state: present
